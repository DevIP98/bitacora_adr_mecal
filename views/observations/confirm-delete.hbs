<div class="container mt-4">
    <div class="card border-danger">
        <div class="card-header bg-danger text-white">
            <h4><i class="bi bi-exclamation-triangle"></i> Confirmar Eliminación de Observación</h4>
        </div>
        <div class="card-body">
            <div class="alert alert-warning">
                <p><i class="bi bi-exclamation-circle"></i> <strong>Advertencia:</strong> Esta acción no se puede deshacer. La observación se eliminará permanentemente del sistema.</p>
            </div>
            
            <h5>Detalles de la Observación:</h5>
            <div class="mt-3">
                <p><strong>Niño/a:</strong> {{observation.child_name}} {{observation.child_last_name}}</p>
                <p><strong>Fecha de la observación:</strong> {{formatDate observation.observation_date}}</p>
                <p><strong>Registrada por:</strong> {{observation.observer_name}}</p>
                <p><strong>Tipos de observación:</strong> 
                {{#each observation.observation_types}}
                    <span class="badge bg-secondary me-1">{{this}}</span>
                {{/each}}
                </p>
                <hr>
                <div class="description-preview">
                    <p><strong>Descripción:</strong></p>
                    <div class="border rounded p-3 bg-light">
                        {{{formatQuestionsAndAnswers observation.description}}}
                    </div>
                </div>
            </div>
            
            <div class="d-flex justify-content-between mt-4">
                <a href="/children/{{observation.child_id}}" class="btn btn-secondary">
                    <i class="bi bi-x-circle"></i> Cancelar
                </a>
                <a href="/observations/delete/{{observation.id}}" class="btn btn-danger">
                    <i class="bi bi-trash"></i> Eliminar Definitivamente
                </a>
            </div>
        </div>
    </div>
</div>
